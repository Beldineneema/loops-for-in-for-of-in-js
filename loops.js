const names = ["tables", "chair", "phone", "book"];

//for (const n of names) {
    //console.log(n);
//}

  const symbols = {
    t: "tables",
    c: "chair",
    p: "phone",
    b: "book",

};

for(const n in symbols) {
console.log(n);
}